# نسخة Node.js البديلة (مرجعية فقط)

هذا `server.js` بديل كامل لمجلدَي `api/` و`lib/` — يقوم بنفس الوظائف بالضبط (يقرأ/يكتب نفس بنية JSON) لكن باستخدام Node.js بدل PHP+MySQL، ويخزّن البيانات في ملف `data/store.json` بدل قاعدة بيانات.

**غير مُستخدَم حالياً في الإنتاج** (المشروع الآن يعمل بـ PHP+MySQL على `hr-alsalman.com`) — موجود هنا فقط كمرجع تاريخي أو كخيار بديل إن أراد أحد الانتقال لسيرفر داخلي بدل الاستضافة المشتركة.

يعمل مع **نفس ملف `index.html`** دون أي تعديل — الواجهة تتصل بأي واحد منهما شفافياً حسب ما هو متاح على نفس النطاق.
